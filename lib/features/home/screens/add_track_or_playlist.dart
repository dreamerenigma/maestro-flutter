import 'package:flutter/material.dart';

class AddTrackOrPlaylist extends StatelessWidget {
  const AddTrackOrPlaylist({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
